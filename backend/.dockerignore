# The virtualenv, which is a developer's and is the wrong architecture anyway.
virtual/
.venv/
venv/

# Rebuilt inside the image; a stale host copy must not be shipped.
staticfiles/

__pycache__/
*.py[cod]

# The development database. Never ships.
*.sqlite3
db.sqlite3

# Secrets.
.env
*.env
!.env.example

.git/
.gitignore
